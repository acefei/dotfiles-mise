#!/usr/bin/env bash
#MISE description="Point ~/.vimrc at this repo's vimrc, install plugins"
set -euo pipefail
source "$MISE_PROJECT_ROOT/lib/utils.sh"

ensure_line "source $MISE_PROJECT_ROOT/config/vim/vimrc" "$HOME/.vimrc"

"$MISE_PROJECT_ROOT/config/vim/plug_installer"
